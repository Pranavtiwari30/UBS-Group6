name: payment-exception-orchestrator
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - "pydantic>=2,<3"
      - "pytest>=8,<9"
      - "rich>=13,<14"
      - "openai>=1.40"
      - "fastapi>=0.100.0"
      - "uvicorn>=0.23.0"
